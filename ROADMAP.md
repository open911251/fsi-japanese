# ROADMAP

2026-07-13 討論定案。記錄 FSI/ALM 教學法的已知限制、對應解法與實作順序。

## 四個已知限制與對策

| 限制 | 對策 |
|---|---|
| 缺複習排程 | 間隔複習（spaced repetition）：localStorage 記每句練習紀錄，Leitner 盒排程，按「開始」時先插入到期複習題 |
| 操練順序固定 | 代換 cues 與 qa 題目加「隨機順序」開關（shuffle），避免背順序而非句型 |
| 純機械操練 | 新增「開放應答」模式：答案不唯一，由 LLM 評判（有無對題、文法對錯、是否用到本課句型）並給修正建議 |
| 無正誤回饋 | 拆兩半：**正確度**用 STT（Whisper）轉文字後比對／LLM 評判，可解；**腔調**維持現有音高曲線 A/B 對比——Whisper 原理上會丟棄韻律資訊，不能用於腔調判斷 |

## 已定案的架構決策

- **Google Cloud TTS 輸出端不動**。選用理由是自然度，示範音質決定模仿上限；免費額度足夠。新功能全部在輸入端（聽使用者說什麼），與 TTS 互不相干。
- **推理後端＝使用者工作站的本地 LLM**（使用者日常以 ssh 連線，機上有較大模型）：
  - `ssh -L 11434:localhost:11434 工作站` 轉發 OpenAI 相容 API（Ollama/llama.cpp/vLLM）
  - 瀏覽器視 localhost 為安全來源，GitHub Pages（HTTPS）直連 `http://localhost:*` 不被 mixed content 擋
  - CORS：Ollama 需設 `OLLAMA_ORIGINS=*`（llama.cpp/vLLM 各有對應開關）
  - 手機使用場景日後再考慮 Tailscale，暫不處理
- **STT 也自架**：工作站跑 faster-whisper / whisper.cpp server 模式，網頁把現有 MediaRecorder 錄的 webm POST 過去。繞過 iOS Safari 無 Web Speech 辨識的問題，零 API 費。
  - ✅ 2026-07-13 已部署：工作站使用者空間跑 FastAPI（faster-whisper large-v3-turbo GPU），同一 port 同時提供 STT 與 Ollama 的 CORS 代理，共用 Ollama 設定不動；只綁 127.0.0.1，經 ssh 隧道使用（單一 port 轉發即可）。
- **網頁端只新增設定欄位**：「LLM endpoint URL」「STT endpoint URL」，存 localStorage（與 TTS key 同模式）。維持單一 HTML 檔、不引入外部依賴。做 OpenAI 相容格式，同欄位填雲端 API 也能用。
- **母語者校對以 LLM 校對替代**（成本考量）；自然度終審用前沿模型，本地模型只做初篩。
- 腔調回饋的強化方向（低優先）：比對該句的標準東京式重音型（OJAD／UniDic 重音資料），而非換掉音高對比法。

## 方向修訂（2026-07-13）

**目標＝在日生活能力，JLPT 為輔。** 影響如下：

- 內建文法課推進到 N3 收尾即可，不做 N2 長尾句型的陣地戰（考試導向的閱讀量瓶頸不是本工具的戰場）
- 擴充方向改為**生活場景課**：市役所・郵局手續、醫院、電話應對、購物退換貨、租屋、職場基本敬語應對——每場景高頻固定句＋應答練習，高度可操練
- 真實語速聽力（縮約口語、快速敬語）靠真實材料＋「自訂教材」貼句操練，不自產
- 開放應答模式（第 4 項）升級定位：做成生活場景的模擬對話
- 優先序：間隔複習（5）→ 開放應答（4）→ 生活場景課

## 實作順序

1. ✅ 教材 LLM 校對（2026-07-13 完成，一次性）：28 課全數檢查，無需修正的硬錯誤；少數風格備註見當日討論紀錄
2. ✅ 代換／應答順序隨機化（2026-07-13 完成）：設定區「隨機順序」開關，影響代換 cues（各句型內，基本句仍在前）與應答題序；一輪之內順序固定（seed 快取），每輪重洗；存 localStorage
3. ✅ LLM／STT endpoint 設定欄位 ＋ 正誤回饋（2026-07-13 完成）：設定卡「本地 AI 設定」收 STT／LLM 的 URL 與模型名（OpenAI 相容、存 localStorage）；「正誤回饋」開關開啟後，代換・應答的留白期間錄音 → POST 到 STT → Levenshtein 相似度比對（正規化：去標點、私/わたし・下さい/ください・阿拉伯/漢數字歸一，並同時比對假名欄取高分）→ 顯示 ✅≥85%／🔶≥60%／❌，附辨識結果與正解。辨識在背景進行不阻塞播放。LLM 欄位供第 4 項使用
4. ⬜ 開放應答模式（LLM 評判 + 修正建議）
5. ✅ 間隔複習排程（2026-07-13 完成）：Leitner 五盒（間隔 1/2/4/8/16 天），每句一筆存 localStorage（`fsi_srs`）。按「開始」從頭跑時自動插入至多 5 句其他課的到期複習題（同模式），進度列標「🔁 複習」與出處課名。正誤回饋開啟時由辨識結果升降盒（≥85% 升、<60% 打回盒0），關閉時播完即視為成功；未到期的重複練習不改排程（防止一天狂刷衝頂）。build 與自訂教材不排程
6. ✅ 代換詞預習（2026-07-13 完成）：「代換詞預習」開關，開啟後每個句型在基本句之後插入 `{type:"prev"}` 項目——唸代換詞→播解答句（含假名中文），不要求產出（仿 FSI breakdown，操練不撞生詞）。與隨機順序並存（prev 維持原順序）、不進 SRS。詞彙廣度不歸本工具管（交給 Anki 等，本工具只顧操練流暢）
7. ✅ 回合評分與課程順序（2026-07-13 完成）：回饋開啟的 sub/qa 每輪結算平均分（roundVerdict，≥75 合格）；合格建議下一課、未達建議重練（不強制鎖課）；每課 best/last 存 `fsi_score`，課程選單顯示徽章（取 sub/qa 較弱一項當精熟下限）。遲到的 STT 結果會補進結算。2026-07-14：build 逐段跟讀也接上評分（每段與段落文字比對，末段用 full 假名；不進 SRS），徽章精熟下限含 build
8. ✅ 操練中腔調圖（2026-07-13 完成）：回饋開啟時，listen 跟讀留白也錄音，並在 fbPitch() 沿用⑤的音高管線（pitchTrack/contour/drawPitch 加 canvas 參數）於操練畫面直接畫示範 vs 使用者曲線＋相似度＋語速差；無 TTS key 靜默略過
9. ✅ 差異區段重點重聽（2026-07-14 完成）：worstWindow() 以 8/48 點滑窗找平均半音差最大處，畫布標紅底，fbSeg 記錄示範音檔時間切片（contour 補回傳 t0），「🐢 重聽差最大的一段」以 Audio currentTime＋preservesPitch 半速播放該切片。平均差 <2 半音不顯示。不用 OJAD（無官方 API、音質低於 Neural2）
10. ✅ 文字修正建議（2026-07-14 完成）：fbHints() 把曲線差異翻譯成指令（句首高低、句尾該降沒降／該揚沒揚），附在腔調資訊列
11. ✅ 聽辨訓練分頁（2026-07-14 完成）：⑥ 最小對立組測驗（MINPAIRS，7 組名詞對：頭高/尾高/平板），TTS 唸「〇〇です」句、二選一、答後強制對比播放；成績存 `fsi_minpair`。Kotu.io 無 API 且 iframe 會被擋，故原生自建；大量刷題仍建議 Kotu
12. ✅ 卡拉OK式即時跟唱（2026-07-14 完成）：解「腔調矯正不及時」。回饋開啟時每個開口留白都是跟唱畫面——liveStart() 先畫示範曲線當軌道（liveModel() 以 text|voice|rate 快取 contour），AnalyserNode 每 45ms 取 2048 樣本跑 pitchOfFrame()（自相關 70–420Hz、RMS 門檻），游標從使用者開口那刻起跑、掃完示範時長，音高以使用者自己的滾動中位數正規化成半音疊畫上去。說完由既有 fbPitch() 事後對比接手。全瀏覽器本地。listen/sub/qa/build 全部生效
13. ✅ 今日條（2026-07-14 完成）：頁首狀態驅動儀表——連續天數（`fsi_streak`，完成任一輪操練記為當日有練）、到期複習總數（`srsDueAll()`，一鍵切到最多到期的模式開跑）、上次進度（`lastPracticed()` 掃 `fsi_score` 時間戳，≥75% 顯「繼續」否則「重練」）、聽辨提醒；自勾清單（`fsi_daily`，跨日歸零；操練自動勾、Anki／聽力手動）＋漸進揭露面板（點文字展開 2–3 個外部連結與「卡句貼回自訂教材」提醒，點方框只打勾）。原則：狀態驅動的今日待辦，不做靜態功能牆
14. ✅ 聽辨訓練批量擴充・音段對立（2026-07-16 完成）：MINPAIRS 從 7 組擴到 29 組，新增三類「差異寫在假名上、TTS 必唸得出」的低風險對立——長短音・拗音 9 組、促音 6 組、清濁・半濁 7 組（選詞避開多音字如「音／おん」，兩句載體相同、只差目標詞）；抽題改 `mpPick()` 依各組答錯率加權（`fsi_minpair` 新增 `g` 欄記每組成績，舊資料相容），錯得多的組出現率約為熟練組 4 倍、沒練過視為 50%；test-minpair 放寬為「重音符號或音段類別詞」雙軌標記並加抽題統計驗證
15. ✅ VOICEVOX 音高資料整合（2026-07-18 提案並完成，使用者發現）：`audio_query` 回傳逐拍（mora）重音結構、`synthesis` 可吃改寫過的 accent 值重新合成。設定卡新增 VOICEVOX URL／speaker 欄位（`fsi_vv_url`／`fsi_vv_speaker`，同 localStorage 模式）＋「測試」按鈕；工作站 VOICEVOX engine 改用 `--cors_policy_mode all` 啟動（已重啟，實測 GitHub Pages 來源的 preflight 回 200）。三個子功能：
    - **A．逐拍評分**：`vvMoras()` 展平整句 accent_phrases，每拍高低取「該 accent phrase 內有聲拍平均以上＝高」（不查教科書規則表——實測「箸です」等頭高詞，字典規則的「核後立即下降」跟引擎實際音高常差一拍即「尖峰延遲」現象，規則反而會誤判；直接用引擎輸出更貼近使用者聽到的示範）。`moraScore()` 比對使用者 `contour()` 的 48 點半音序列，逐拍標記吻合／不吻合。掛到 `fbPitch()`（操練中）、`analyzePitch()`（⑤錄音對比）：canvas 疊金色理論高低階梯（`drawMoraTrack`），下方新增逐拍列（`fbMoraRow`／`pitchMoraRow`，`renderMoraRow`）
    - **B．重音位置聽辨**（⑥分頁新分頁籤，實驗性）：不需人工找同音詞對——任一詞查 VOICEVOX 拿正確 accent，複製 JSON 改寫 accent 欄位合成一個「錯誤」版本，二選一問「哪個是標準唸法」。詞庫 `ACCENT_WORDS` 20 詞（含 MINPAIRS 既有 10 詞＋10 個新常見詞），成績存 `fsi_accentpos`
    - **C．跟唱理論軌道**：`liveModel()` 快取 VOICEVOX moras、`liveDraw()` 疊同一條理論階梯，示範曲線＋使用者即時曲線＋理論高低三線同框
    - 2026-07-19 設計覆核後修三處：①`moraScore` 使用者端門檻改按 accent phrase 分組取平均（原全句 0 門檻與理論端不對稱，長句自然下傾時後段 phrase 的相對高拍會被誤判成 L，已加下傾情境測試）；②⑥答題標籤改 `apWordLabel()` 還原詞級重音（平板詞＋です核固定落在「で」＝a=n-1 的規律，實測 20 詞全部吻合辭典；原 `accentLabel` 直接套整句會把平板⓪詞標成中高③誤導使用者）；③⑥出新題時 revoke 上一題的 blob URL（原本每題洩漏兩個）。已知未修的小項：⑥答題前不能重聽 A/B
    - 2026-07-19 **發現並修復⑥重大 bug**：`/synthesis` 只讀 `moras[].pitch`、不看 `accent` 欄位（工作站實測：只改 accent 合成音 MD5 完全相同）——⑥上線以來 A/B 播的是同一個聲音。修法＝改 accent 後先過 `/mora_data` 重算音高（`vvRecalc()`），重算後音檔確實不同。同時新增「🧪 評分自檢」按鈕（`vvSelfTest()`）：把正確重音與改錯重音的合成音當假使用者跑正式評分管線，輸出「正確版綠率（目標≥90%）／錯誤版抓錯率（目標≥80%）」——這是腔調學習履歷（第 17 項）開工前的可信度量化關卡，使用者按一次即得數字
    - **測試狀況（供下次接手判斷）**：✅ 已測——`tests/test-voicevox.js`（vvMoras／accentLabel／moraScore 邏輯，含用真實引擎回應「箸です」build 的 fixture，驗過尖峰延遲現象）；ssh 直連工作站 VOICEVOX 驗證 audio_query／synthesis／accent override 契約與 CORS header；20 詞聽辨詞庫逐一在真實引擎跑過 query＋雙版本合成；既有 5 個測試檔（validate-lessons/test-shuffle/test-feedback/test-srs/test-minpair）跑過無回歸。**⚠ 未測**——沒有瀏覽器自動化工具，實際瀏覽器點擊流程（設定卡新欄位存取、canvas 疊圖視覺呈現、⑤逐拍列、⑥「重音位置」分頁按鈕互動、跟唱三線疊圖）完全沒有人工跑過；`moraScore` 在真人錄音（非合成音）上的準確度未驗證，僅有邏輯層 unit test。**使用者下次開啟頁面時應先實際點過一輪再視為正式驗收**
16. 🔶 日語陪聊 bot（`bot/`，2026-07-16 設計定案見 bot/DESIGN.md；2026-07-17 v1 文字版程式完成——agent.py 已在筆電實測抓到前景視窗，bot.py（含先生モード雙模式）語法檢查通過、待工作站部署實測，部署步驟見 bot/README.md）。**核心目標＝語音直接對話**（非學習時間掛著娛樂時無壓力閒聊）：v2 Discord 語音頻道管線（voice.py：discord-ext-voice-recv 收音→停頓 0.8s 斷句→faster-whisper→同一套人設→VOICEVOX 播回；barge-in、語音不糾錯只默默記 errors.jsonl）。
    - 2026-07-17 部署進度：v1 文字版＋v2 語音全部部署到工作站並上線（DM 指令「おいで」進語音頻道／「ばいばい」離開；tmux session：`fsibot`＝bot、`voicevox`＝TTS engine 0.25.2 於 50021）。人設已加台詞例＋大人對話不說教規則＋min_p/repeat_penalty 取樣；模型 gpt-oss:120b（嫌慢可換 gemma4:31b-it-q8_0），聲線 VOICEVOX speaker 11 玄野武宏。拒答常見再考慮 abliterated 模型。
    - 2026-07-18 **v2 語音實測成功**（完整對話迴路通）。過程中發現 Discord 2026-03 起強制語音 E2EE（DAVE），Python 收音生態未支援（discord-ext-voice-recv 只收到亂碼），改為混合架構：`bot/relay/relay.js`（discord.js＋@discordjs/voice 0.19.2＋davey，tmux `fsirelay`）當耳朵嘴巴——收音、AfterSilence 800ms 斷句、播放、barge-in；大腦仍在 bot.py（HTTP 8789 `/voice/utterance`、`/voice/greet`，控制 8790）。坑：Discord ID 超過 JS 安全整數，config 的數字型 id 會被 JSON.parse 四捨五入，relay 以 regex 從原文抽字串解決。人設微調（禁問句 6 成機率、禁空洞相づち、intonationScale 1.15）、聲線切換琴詠ニア（74）
    - VOICEVOX engine 現由訓練工具（第 15 項）與 bot 共用，注意改 CORS/啟動參數時兩邊都要重驗
17. ⬜ 腔調學習履歷（2026-07-19 使用者提案）：把 moraScore 的逐拍結果按重音型分類累積（平板/頭高/中高/尾高 各記 n/ok，加三個診斷錯誤型：核後該降沒降、平板被加下降、首拍該低讀高；按日分桶存 localStorage 保留 30 天）。回饋兩路——①進步偵測：近 7 天 vs 之前的正確率比較，顯著進步在回合結算/今日條稱讚（腔調進展慢，可視化進步對維持動機關鍵）；②弱點導向：最低正確率類型顯示於⑤，並餵給⑥重音位置聽辨抽題加權（同 mpPick 邏輯）。前置：vvMoras 需多帶各 phrase 的 accent 核位置；**先確認使用者實際體感 moraScore 判定夠準再做**（比例對齊誤判多的話先上 DTW 對齊，否則統計會被污染）。2026-08-05 補充提案（矯正中心）：③生成導向——弱點類型不只拿去加權⑥既有題庫抽題，開「矯正中心」時額外呼叫 LLM **針對該弱點類型現生新句子/新練習內容**（例如平板詞常被加下降，就生一批平板詞例句練習），背景慢慢生不用即時、沒有播放中的延遲壓力。實作順序不變：仍要先過「🧪評分自檢」的準確度門檻才能啟動整個第17項（含這個新提案），避免矯正方向建立在不準的判定上
18a. ⬜ 操練種類擴充（2026-08-05 使用者提案）：FSI標準操練法不只代換/應答/擴展三種，盤點過這個工具目前用①②③④四種模式對應到「模仿(聽讀)／代換／應答／擴展(逐段累加)」四種標準操練，缺的幾種——**轉換操練**（給句子＋轉換指示如肯定→否定/現在→過去，自己套規則變出來，跟代換操練不同在於是規則內化不是詞彙代入）已拍板要做，第1課試點中（沿用qa模式的播放引擎跟資料格式，只是新增`trans`欄位＋第5個模式tab）。其餘記在這裡以後再議：**轉述操練**（第一人稱句轉「他說〜」間接引述，要等到N4文法點如第18課「と思います／と言いました」才有意義，之後擴充到N4課次時可以一起做）、**替換移位操練**（除了換詞還要連語序/助詞位置一起調整，優先度低，日文語序調整對N5-N4學習者是否必要待評估）、**連鎖操練**（一人答完接下一人問，單人自學無法真正做多人接力，⑧角色扮演的AI輪流問答是最接近的替代品，不用另外做）、**完成操練**（給半句自己補完，跟代換操練功能重疊度高，優先度低）
19. ⬜ 重音對立批量管線（kanjium）：優先度降低——第 15 項 B 子功能已用「任一詞查 VOICEVOX＋改寫 accent 合成錯誤版」達成類似訓練效果，不再依賴人工找同音詞對。若仍要做傳統「同音不同重音」詞義對立組（MINPAIRS 風格），流程不變：從 kanjium `accents.txt`（Yomichan 音調資料，約 12 萬詞條）按讀音分組撈詞對 → LLM 只填中文釋義 → 現在可直接用 VOICEVOX 指定重音合成，免 TTS 驗收這一步

20. 🔶 成長回饋／綜合測驗模式（2026-08-09 討論定案並實作，細節見 CLAUDE.md「綜合測驗（⑨分頁）」段落）：討論起點是「現有回饋多是瞬間分數，缺『啊我真的變強了』的感覺」，參考 Duolingo 一類 app 的 small-wins／goal-gradient 設計與 MARU 五十音 app 的「上色收集卡」機制後收斂出兩塊：
    - **A．⑥聽辨頁卡片牆**：`MINPAIRS` 是固定小集合（現29組），`mpHist.g[key]={n,ok}`（`fsi-japanese-trainer.html:2219,2244`）已經在記每組正確率，只缺視覺化——把現在藏在 `mpScoreShow()` 一行文字裡的歷史正確率，改畫成一格一組、答對率越高顏色越深的卡片牆，呼應 MARU 的收集感。SRS 逐句複習不適用這招（不是封閉集合，沒有「收集完」的終點）
    - **B．獨立「綜合測驗」入口**（2026-08-09 設計定案，細節如下）：跟「今日條」平常操練分開——各模式（sub/qa/build/trans/listen/⑤跟讀/⑥聽辨）日常練習的評分照舊，只當「這輪要不要繼續練」的建議信號，不受這項影響。新增獨立測驗入口，題目從**所有已做過的評分型操練**隨機混抽（跟讀腔調對比、聽辨對立組、應答…）
    - **已收回的兩個提案**（討論中證偽，記錄避免以後重新踩雷）：①聽辨頁改二選一為打字驗證——會把「音韻辨識能力」跟「假名拼寫能力」混在一起，答錯到底是沒聽出差異還是打錯字，測驗失真；②二選一改三選一＋不相關詞當干擾項（oddity task）——不相關詞不用聽細節就能刷掉，反而比二選一更簡單，且測的是另一種能力（一般詞彙辨識而非精細音韻辨識）。最小對立組的正規方法本來就是二選一辨識/區辨任務，不需要為了「不膩」犧牲效度；audio-lingual method 也明訂操練中不引入書寫，核心四模式（sub/qa/build/trans）口說形式不受這項影響
    - **顆粒度＝句型**：用 `sub` 現成的 `p` 欄位（句型模板，如「わたしは＿＿です。」，`fsi-japanese-trainer.html:400`）。現況實測：28課目前只有約41組（第1-8課每課2-4組，第9-28課因為第18a項提到的擴充還沒做，每課只有1組）
    - **⚠ 前置依賴（無自動觸發，容易被忘記）**：第9-28課補句型是照 CLAUDE.md「擴充教材的標準流程」走（草稿→獨立 agent 校對→測試→才寫入 `LESSONS`），工具本身無法偵測使用者練到哪課就自動生內容，純靠人主動啟動。這件事本來排序模糊，但格子牆一旦做出來，20/28課會長期卡在「每課只有1格」看起來很空——**這算是格子牆這項的前置依賴，不是可以無限期擱置的獨立待辦**，之後真的要動工這項時，第9-28課句型擴充要排在格子牆UI實作之前或至少同批做，不要等格子牆做完才發現內容撐不起版面
    - **實測補充（2026-08-09）**：第9-28課不只句型數少，代換詞（`sub[].cues`）深度也一起單薄——全部卡在每課1句型、每句型剛好4個代換詞的最低骨架量；已補完的第1-8課（commit `552ce3d`）平均每句型3.3-8個代換詞，深度豐富得多。全教材代換詞實測共182個（不是句型數41）
    - ✅ **2026-08-09 第9-13課已補完**（獨立agent審查文法範圍/假名/語感/撞句皆通過）：第9課補「上手です」「＿＿が少しわかります」「＿＿ですから」3個句型，第10-13課各補2個句型（います/位置詞、期間/〜回、でした/より、がほしいです/Vに行きます）。全教材句型數41→52、代換詞182→226。**第14-28課仍未補**，是同一類前置依賴，繼續照CLAUDE.md的五步流程小批擴充，不要一次衝完
    - **格子牆＝全貌，測驗範圍＝獨立解鎖窗口**：格子牆一開始就畫出全部28課／所有句型（多數灰色未解鎖），呼應「沒學過答錯不傷自尊」——因為根本還沒進測驗池，不會被抽到。但測驗池本身**不是**綁「目前學到第幾課」，是這個測驗模組自己的窗口：初始給前2課（暫定，待實測調整），窗口內**每一課各自**正確率達標（暫定沿用既有 `roundVerdict` 的75%合格線，不能用整個窗口平均——平均會讓表現好的課蓋住表現差的課，反而讓真正要抓的「假性學會」漏洞躲過去）才解鎖下一課，每次解鎖暫定+1課
    - **視覺只升不降＋背景弱點加權**：格子顏色比照 `scores[lesson][mode].best` 既有的「只認歷史最佳」邏輯，只會變深不會因後續答錯而變淺，避免打擊信心；已解鎖範圍內若之後又錯，不影響顯示顏色，但比照 `mpPick()`（`fsi-japanese-trainer.html:2219`，`w=1+3*答錯率`）的加權抽題邏輯，暗地提高該句型後續被抽到的機率，兩條線互不干擾——弱點加權不會反過來卡住解鎖進度
    - **待實測調整的數字**（非架構問題，之後跑起來再校）：初始窗口大小（暫定前2課）、每次解鎖增量（暫定+1課）、解鎖門檻（暫定75%）
    - **內部除錯 log（不對使用者顯示，純供事後丟給 AI 核對評分邏輯用）**：比照第17項 `vvSelfTest()`「🧪評分自檢」的精神——之前就是靠留夠細的資料才抓出 VOICEVOX `/synthesis` 不看 accent 欄位、A/B其實同一個聲音這種評分邏輯本身的 bug，這裡預先比照辦理。新開 localStorage 鍵（暫定 `fsi_examlog`），每次測驗作答存一筆事件（非只累加 `{n,ok}` 聚合），上限筆數 500（FIFO，滿了砍最舊）：
      ```
      {t:時間戳, lesson:幾課, patternIdx:句型索引, mode:來源模式(sub/qa/build/trans/minpair...),
       raw:{sim:相似度%, threshold:當時判定門檻, sttText/expected:辨識結果 vs 正解文字(有STT時)},
       hit:對/錯, levelBefore:熟練度階層(前), levelAfter:(後),
       windowChanged:有無觸發解鎖, unlockedLesson:有的話是第幾課}
      ```
      重點是 `raw` 留判定當下的原始訊號（相似度數字／當時門檻／STT辨識文字vs正解），不是只存最後 hit 的結論——之後若懷疑「這題明明講對了怎麼判錯」，可以直接回頭核對那筆的 `raw`，不用靠記憶重現
    - **實作範圍與已知限制（2026-08-09 auto mode 當下實作）**：B項的題目來源當時定案為只抽 `sub` 代換題，**不混跑讀腔調/聽辨/應答**，理由與狀態見下方「2026-08-09 事後討論修正」——這個範圍決定後來在事後討論中已修正，程式碼尚未同步。✅ 已完成：A/B 兩項的資料模型、抽題加權、窗口解鎖、只升不降的熟練度階梯、除錯log，皆用 `tests/test-examwall.js` 做邏輯層驗證（含「平均會誤判達標」這個最關鍵的情境）；⑥聽辨頁另加 `mpWallRender()` 卡片牆。**⚠ 未測**：跟 15 項 VOICEVOX 上線時一樣，沒有瀏覽器自動化工具，實際點擊流程（⑨分頁的錄音/STT/出題節奏、格子牆視覺呈現、⑥新增的卡片牆）完全沒有人工跑過，**使用者下次開啟頁面時應先實際點過一輪再視為正式驗收**。第9-28課 `sub` 句型缺口（每課僅1句型4代換詞）仍未補，這些課的格子牆會很稀疏，是已知前置依賴非本次遺漏，見上面「實測補充」條目
    - **2026-08-09 事後討論修正（程式碼尚未同步更新，下次動工先看這條）**：
      1. ~~作答互動改成使用者主動觸發~~（**已反悔收回**：原本擔心跟聽辨A/B介面混亂想改手動按鍵，但後來想到第4點加了「先播base示範」這個緩衝步驟後，唸完提示詞再自動錄音就不會突兀，維持自動留白錄音（比照 `fbGap`）即可，不需要額外的手動錄音鍵；qa/build/trans 這幾種本來就有完整題目文本、沒有代換題的歧義問題，也維持自動留白錄音，跟現有練習模式一致。只有聽辨維持A/B點選，跟開口類是不同作答方式，不衝突。**目前 `examAsk()` 現有的自動錄音寫法不用改這條**）
      2. **題目來源擴大為 sub+qa+build+trans**：這四種都是按 `LESSONS[i]` 課次組織的（跟 `sub` 結構相同），可以直接一起餵進同一個窗口解鎖判定（`examWin.p[lesson]` 本來就已經是課次層級在算，不管題目來源），**不需要新增格子牆格子**——格子牆維持只有41個句型格，qa/build/trans答對答錯只影響該課的解鎖進度，不會多長出新的可視化項目，避免總項目數暴增到531個（`validate-lessons.js` 算出的全教材操練項目總數）破壞「一眼看到全貌」的收集感
      3. **聽辨MINPAIRS也納入同一套窗口**：使用者提案「聽辨反正沒有課次問題，乾脆平分進28課」——幫每個 `MINPAIRS` 項目加一個人工 `lesson` 標籤（輪流分配，不代表真實課程關聯，純粹借用窗口機制），答對答錯算進該課的窗口計數器，作答介面維持A/B點選（不是STT錄音），跟A項的獨立卡片牆並存不衝突
      4. **代換題的句型歧義問題（重要，關係到題目能不能被正確判定）**：實測（node直接算 LESSONS 資料）發現 154 個相異提示詞裡有 **21 個會跨句型撞名**，尤其第9-28課「變化練習」類句型的提示詞是裸動詞ます形（如「飲みます」），會同時出現在多個完全不同的變化方向（第13課→たいです、第14課→て形、第15課→たことがあります），不給任何上下文的話題目本身沒定義清楚要考哪個方向。**解法不是顯示句型名稱標籤**（原提案「句型：ます形→たいです」被使用者否決，理由是要求使用者認知/記憶語法術語違背FSI直接操練精神）——改成抽到句型的提示詞前，先播一次**同句型的 `base` 轉換示範**（例如先播「食べます → 食べたいです。」示範轉換方向，再播提示詞「飲みます」要使用者自己套用同樣轉換），這跟②代換模式本身連續練習時「先播基本句再進提示詞」的既有邏輯完全一致，純粹靠範例示範、不靠術語命名
      5. **`build`（逐段累加）暫時從題目來源移除**：`sub`/`qa`/`trans` 每課都是一堆獨立題目的陣列，隨機抽一題很自然；但 `build` 每課只有一組 `{full,parts}`（從短到長逐步累加的同一句話），是一整條漸進式階梯，不是「一堆題目」，還沒想清楚怎麼變成測驗池裡的一題（抽階梯裡的某一段來考？還是跳過鋪墊直接考完整句？），之後再議，這次先不含 build
      6. **`qa`／`trans` 的考法定案**：兩者都不需要像代換那樣額外處理歧義——`qa` 每題是完整的一問一答（播問題已經講清楚要考什麼），`trans` 的指示句本身就是完整日文句子（已明講要做什麼轉換）。流程直接比照各自現有的③應答／🔁轉換模式：播問題/指示句 → 自動留白錄音 → STT比對答案文字，不需要額外示範步驟
      7. **qa 評分的已知限制與未來優化方向（不卡這次動工）**：`qa` 是開放式問答，可能有多種語意/文法都對但講法不同的答案，資料庫目前每題只存一個標準答案，STT文字相似度比對可能誤判「講法不同、語意對」為錯——這比使用者困惑更嚴重，因為窗口解鎖判定的可信度建立在這個對錯訊號準確的前提上。當下決定：接受現有單一答案比對（跟日常③應答練習的準確度一致，沒有變差），靠窗口解鎖至少 `EXAM_MINN`（4）次樣本的機制讓少數誤判被平均掉，不單靠一次誤判卡住解鎖；答錯時仿照 `fbCheck` 顯示「你說的／預期的」對照文字（不是暴露計分規則，是內容回饋，跟前面「不顯示規則」原則不衝突），讓使用者自己判斷是真錯還是講法不同，措辭避免用「錯誤」這種肯定語氣。未來兩個優化方向記錄於此，都是內容工程或需要額外設計，不卡這次動工：
         - **qa 比照⑧角色扮演的 `accept` 陣列設計**，把單一答案擴充成多重可接受講法，判定時比對到陣列任一句就算對——這是內容工程（要把28課112題qa重新審一遍），跟第9-28課句型擴充同一類，需要獨立agent審查流程
         - **加「申訴」鈕**：使用者對判定有嚴重異議時主動按鈕觸發，把這題的 `raw`（`examlog` 裡已經存的 sim/threshold/sttText/expected，不用另外準備資料）送給使用者已設定的LLM複核。這跟⑧角色扮演否決「LLM即時裁判」不衝突——那邊否決是因為每題都要即時判、不穩定+慢會卡住操練節奏；這裡是使用者主動、非阻塞觸發的事後複核，沒有時效性壓力。申訴成立要回頭修正三處：這題的 `hit`（錯翻正）、`examMastery`（補升一階）、`examWin.p[該課]` 的 `ok` 數（可能因此補觸發原本卡住的解鎖判定）
    - **2026-08-09 獨立審查（第一輪，軟體工程視角）發現兩個結構性缺口**：①解鎖判定用終身累積比率，已解鎖課次事後表現退步（正常語言遺忘）會讓比率跌破門檻，因為判定要求「所有已解鎖課都達標」，會靜默卡死後續所有解鎖且使用者無從得知原因（規則本就不對外顯示），且n=4時統計抖動大，解鎖課數越多、所有課同時達標的機率是連乘積，會隨進度指數式惡化；②規劃中「聽辨併入窗口」（上面第3點）會讓50%猜測基準的任務跟趨近0%猜測基準的產出任務共用同一門檻，答不出完整句子的人可能靠聽辨矇混過關，正好開了這功能最想抓的「假性學會」後門
    - **2026-08-09 獨立審查（第二輪，語言測驗學＋FSI教學法視角）給的解法，皆已採納**：
      - **問題①解法（永久達標旗標）**：「是否曾達標」跟「現在還記不記得」是兩個不同構念，不該用同一個累積比率回答。改成一旦某課首次達標（n≥EXAM_MINN且ok/n≥EXAM_THRESH）就寫入一個**永久不撤銷的旗標**，解鎖判定看旗標不看即時比率；已達標課若之後退步，不影響解鎖資格，改把那題答錯的訊號接到既有SRS複習排程（`srsMark` 降盒），讓退步有明確、對使用者友善的去處，而不是憑空消失或憑空卡住別的東西
      - **問題②解法（聽辨不併入窗口）**：語言測驗理論上，強制選擇題（forced-choice）的猜測基準必須做校正才能跟自由產出題的分數互相比較，兩者測的也是不同構念（接收辨識 vs 產出）——**結論是第3點「聽辨併入窗口」的規劃直接撤回，不要做**。聽辨維持在⑥頁自己的 `mpWallRender()` 卡片牆，跟⑨綜合測驗完全獨立，不共用任何門檻或計數器
      - 主動發現但不影響本次動工範圍的附加建議（記錄供未來參考）：a. 只升不降的顏色牆+錯題加權會讓「早就精熟」的格子長期不再被重新驗證，牆會漸漸變成歷史紀錄——已採納，加了「新鮮度地板」（`EXAM_FRESH_DAYS`，久未被抽到的已精熟項目權重緩慢回升）；b. 強制線性解鎖跟語言習得的非線性節奏有張力，但因窗口只綁測驗池、不綁日常練習能碰哪課，衝突有限，未來可考慮加一句UI提示「這只是挑戰關卡，不影響正常練習進度」降低卡關焦慮（這次未做，屬UI文案優化）；c. `EXAM_MINN=4` 統計上仍偏小，先不重新設計判定公式，之後若實測發現卡關抱怨偏高，優先檢查是不是樣本量問題
    - ✅ **2026-08-09 第二輪程式碼已完成**（`fsi-japanese-trainer.html` exam區塊整段重寫，`tests/test-examwall.js` 同步重寫並新增涵蓋範圍）：題目來源改為 sub+qa+trans（build仍排除，維持第5點的判斷；聽辨仍不併入，維持上面撤回的結論）；`examKey` 改成 `mode|lesson|idx` 統一格式；`examMasteryBump` 只有sub題型才點亮格子（qa/trans只灌窗口進度不長格子）；解鎖判定改用 `examWin.a` 永久旗標（`examMaybeUnlock`/`examWinBump`）；新增 `examSrsKeyFor` 把已達標課退步接進SRS；`examPick` 加了新鮮度地板；sub題目答題前先播 `base` 轉換示範解決歧義；`qa`/`trans` 直接播完整題目文本不需額外示範；答錯顯示「你說的／預期的」對照文字取代單純❌。**全部10個測試檔跑過皆通過（exit 0）**。
    - **2026-08-09 使用者實測發現的問題與修正**（**第一次真的打開瀏覽器測試**，抓到程式碼推演時沒想到的坑）：
      1. **qa 整個移出題庫**：測到「この本は日本語の本ですか。」預期答案「いいえ、英語の本です。」——這種答案是編出來的劇本事實（這本書是不是英文書），沒有圖片/前提脈絡使用者根本無從推理，跟「這句話怎麼講」的語言問題性質不同。日常③應答練習沒事（聽完會播正解句讓你跟讀，不要求答對），但綜合測驗「答對才算過」的假設在這種題目上直接不成立。這比 roadmap 第7點「qa講法不同」的疑慮更根本（不是講法問題，是題目本身答不出來），**qa 已從 `examPoolKeys()` 移除**，`examQaCount()` 一併刪掉（已無呼叫者）；`qa`比照`accept`陣列擴充、配圖等都無法解決這個根本問題（配圖或許可以但工程量遠大於現有範疇，先不做）。目前題庫只剩 sub+trans
      2. **測驗畫面拆成兩張獨立卡片**：原本格子牆（圖例/統計/牆）跟出題互動（按鈕/題目框/結果）擠在同一張 `examCard` 白卡，使用者要求分開，改成 `examCard`（格子牆）+ 新的 `examQuizCard`（出題互動）兩張卡，`switchMode()` 同步切換顯示
      3. **加類別標籤**：使用者實測時把③應答題當成跟讀在做（該回答問題卻覆誦問題本身），因為畫面沒有任何提示這題是哪種類型。加了 `examTypeLabel`（沿用②③🔁分頁既有名稱，例如「③ 應答・回答問題」），不是新規則說明，是借用使用者已經認識的既有詞彙
      4. **加10題一輪的session上限**：原本無限次單題循環（比照⑥聽辨），使用者要求加自動結束機制。`EXAM_SESSION_LEN=10`，`examSessionN` 計數到10後顯示「✅ 本次測驗完成（10題）」、按鈕變「🔄 再測一輪」，**刻意不算分數/不顯示正確率**（沿用「不對使用者顯示計分規則」的原則），格子牆本身（每題都會即時更新）就是唯一的回饋
      5. `tests/test-examwall.js` 同步更新（qa排除、題池計數改3+2=5、新鮮度地板測試加大抽樣次數到20000穩定統計）。**全部10個測試檔跑過皆通過**
    - **仍未做**：申訴鈕、qa的accept陣列擴充（因為qa已整個移出題庫，這條的必要性下降，若之後真要讓qa回歸題庫才需要）、UI卡關焦慮提示文案

21. 🔶 qa配圖（讓 qa 有機會回歸題庫，2026-08-09 討論並驗證生產pipeline，內容本身未生成）：起因是第20項發現「この本は日本語の本ですか」這類qa答案是編出來的劇本事實、測驗答不出來，討論後發現MNN風格教材本來就是配圖建立情境，幾乎所有qa（除了純邏輯/敬語規則類）都能靠配圖變成「照圖回答」而非「編故事」。
    - **分類框架**：需要圖（絕大多數qa，配圖後答案從編故事變成看得到的事實）vs 不需要圖（答案由文法規則/制式應對本身決定，如敬語應對、確認請求）
    - **關鍵原則：yes/no類圖像題要生成對立變體、隨機出題，不能只生單一固定答案版本**——使用者實測時發現「この本は日本語の本ですか」目前只有英文書一種劇本，答案永遠是「不是」，使用者可以不看圖直接背答案矇混過關，這正是⑨綜合測驗最想抓的「假性學會」漏洞、卻是內容設計自己留的後門。修法比照⑥聽辨 `mpAsk()` 的 `mp.side=Math.random()<0.5?0:1` 隨機出題邏輯：日文封面／英文封面兩版都要生，出題時隨機選一個，答案跟著變。這條原則影響「需要幾張圖」的估算——不是每題一張，是每題至少一組對立變體
    - **工作站GPU目前故障**：今天（2026-08-09）重開機後核心版本跳到 `7.0.0-28-generic`，但NVIDIA驅動模組只裝到 `6.17.0-22-generic`，apt套件庫也還沒有對應新核心的590驅動模組，`nvidia-smi` 完全連不上驅動。這台沒裝DKMS不會自動重編，修復需要sudo權限（此帳號無免密sudo）且很可能要重開機（會中斷現有tmux跑的bot/relay/voicevox/STT服務），使用者決定聯絡工作站管理員處理，這條路線擱置
    - ✅ **改走CPU方案並完整驗證**：工作站 `~/imagegen/venv`（獨立venv，不影響其他服務）裝CPU版torch+diffusers，用 `stabilityai/sd-turbo`（2 inference steps, guidance_scale=0）生圖，這顆Intel Core Ultra 9 285K（24核、無AVX512）單張約4-5秒（純生圖，不含judge時的CPU競爭）。裁判模型用Ollama的視覺模型：**先試 `moondream`（1.7GB，圖小應該快）結果完全失敗**——25張測試圖全部被判0分，查出來是模型回應整個是空字串（`eval_count:1` 且 `response:""`），連純文字提示都給破碎輸出，換成 **`llava:7b`（4.7GB）後正常**，能给出0-10有區分度的分數（不是亂數）。**教訓：選視覺模型不能只看體積小/快，要先手動測一次confirm真的有正常回應，小模型在Ollama上可能有模板相容性問題**
    - ✅ **量產loop已寫好並測試（`~/imagegen/batch_gen.py`）**：每個需求生成 `N_CANDIDATES=5` 個候選（sd-turbo 2步）→ 每個候選送 llava:7b 判斷像不像（prompt 要求0-10分，llava有時會答"Yes"而不是數字，判讀邏輯需同時吃數字跟yes/no兩種格式，抓不到時給中性分5分、不要預設0分——0分會讓「解析失敗」跟「圖真的很差」混在一起，之前moondream那次的全0假象就是這樣來的）→ 留最高分、刪其餘4個。測試批次（notebook/umbrella/book_flag/car/person，5需求×5候選=25張）**總耗時413.5秒（約7分鐘）**，最終選中的候選分數都在9-10分，人工抽樣看過car跟person兩張圖確認品質跟分數吻合，圖乾淨可辨識
    - **文字渲染不可靠**：早期測試發現要求生成「封面印英文字」的書會產生糊掉的假文字（不是真英文也不是真日文），這是擴散模型的已知弱點，即使加步數也不保證穩。**解法：avoid文字，改用圖示區分**（例如封面畫國旗/太陽圖案而非文字），已驗證此法可行但圖案本身在極低步數（2步）下結構仍可能不穩定（測過「圓形太陽圖案」讓書的形狀整個消失變成抽象圓環），** 步數與穩定性的權衡還沒細調，目前靠5候選+LLM篩選繞過，不是靠提高單張品質**
    - ✅ **2026-08-09 架構決策＋schema/程式碼已完成**（內容本身仍未生成，這次做的是讓程式碼準備好接圖）：
      - **圖片存放定案**：另存 repo 內 `images/` 資料夾、相對路徑引用，不用 base64 內嵌——base64 在預估規模（套用對立變體原則後上百張）下會讓HTML膨脹到50-100MB+不可行；此舉對「單一HTML檔架構」規則的解讀是「規則擋的是拆分程式邏輯/依賴建置工具鏈，不是零星靜態圖片素材」，**已跟使用者確認過這個解讀，不是我自己假設**
      - **qa 資料格式擴充**（向下相容，現有5欄陣列格式完全不受影響）：新增選填的 `{q,qk,variants:[{img,a,ak,cn},...]}` 物件格式，`qaResolveItem()` 負責正規化兩種格式（純陣列→img:null；variants物件→隨機選一個變體），playItem() 的 qa 分支呼叫它取得 `{q,qk,a,ak,cn,img}`，`showQaImage()` 控制新增的 `#qaImg` 圖片元素顯示/隱藏（`switchMode()` 切分頁時會清空，避免圖片跨分頁殘留）。`trans` 不套用這個機制（維持純文字，指示句本身已經是完整資訊，見上面第6點的既有結論）
      - `tests/validate-lessons.js` 同步更新（接受兩種qa格式），新增 `tests/test-qa-variants.js`（驗證新舊格式解析正確、variants隨機性有效、img跟a不會配對錯）。**全部11個測試檔跑過皆通過**
      - **仍未做**：①實際圖片需求清單重新估算（套用對立變體原則後數量會比原本估的多）；②真正生成正式要用的圖（目前 `images/` 資料夾是空的，`LESSONS` 裡也還沒有任何一筆用新格式）；③第9-13課以外的完整28課逐題分類（目前只示範過L1-L3）；④UI視覺細節可能要再調（`#qaImg` 目前是陽春的 `max-width:220px` 置頂顯示，沒特別設計版面）
    - ✅ **2026-08-09 端到端範例完成**（第1課「陳さんは学生ですか」）：用已驗證的量產pipeline（sd-turbo 2步生圖×5候選、llava:7b打分挑最高）產出「學生」（背包+書本）「上班族」（西裝+公事包）兩版，皆10/10分、人工確認品質清楚可辨識，存進 `images/qa/l1_chin_student.png`／`l1_chin_employee.png`，第1課qa的這題已改寫成新格式並實際帶真圖真答案。**用真實LESSONS資料跑過完整驗證**：`qaResolveItem()` 20次呼叫兩個變體都正確出現、img跟答案配對不會錯、圖檔確實存在於repo。這是第一個真正完工（設計→生產→篩選→寫入→驗證全部走完）的配圖qa題目，證明整條pipeline可行，接下來是照這個模式擴大到其餘題目（不用再重新驗證流程本身）。全部11個測試檔跑過皆通過
    - ✅ **2026-08-09 瀏覽器實測demo成功**：起本機HTTP server（file://被瀏覽器自動化擋掉）載入頁面，切到③應答第1課，實測「陳さんは学生ですか」兩個變體（學生／上班族）都能在真實畫面正確顯示對應圖片與答案文字，非僅node測試層級驗證
    - ✅ **2026-08-09 擴大第二例（第1課「田中さんは先生ですか」）**：老師（學士帽+書）vs 上班族（沿用陳さん那題已有的西裝圖，兩題共用同一張）。**過程中再次踩到文字幻覺坑**：一開始用「黑板+粉筆」當老師的視覺提示，結果黑板上生出一堆看不懂的塗鴉假字——跟書封面那次是同一個失敗模式（「黑板」這個概念本身就誘導模型畫文字，即使negative prompt明講no text也擋不住）。改用「學士帽+畢業袍+書」（不涉及任何書寫平面的視覺符號）後5個候選裡有1個拿到滿分10分，其餘4個都很差（1分），**證明候選篩選機制正在發揮作用，不是每次都穩定成功、需要靠多候選淘汰**
    - **掃過L1-L3後確認：yes/no角色類「安全區」在這個範圍內已經做完（只有陳さん/田中さん這兩組），沒有更多可以無腦擴充的了**。其餘題目分屬四個更難的類別，已找diffusion/prompt engineering專家開會給方案（詳見下一條），這四類方案本身還沒有任何一項實作
    - 🔶 **2026-08-09 四個困難類別的專家方案（僅設計，未實作）**：核心原則——diffusion只做它已驗證唯一穩定的事（單一簡單物件+1-2配件、扁平插畫、無文字），文字/多元素空間關係/跨圖身份一致性這三類已知失敗模式全部移出diffusion職責，改用Pillow做確定性疊圖/排版承接：
      1. **書封面語意標記**（日文書vs英文書）：diffusion只生「素色書」，Pillow疊 Noto Sans JP 字型畫真正可讀的字（不再是diffusion生的塗鴉假字）
      2. **開放式問題（非yes/no）需要3-4個以上變體**：確認不是技術瓶頸是批次規模問題——量化過就算100個物件類別×5候選也只要約1小時生圖+2-3小時llava評分，是「跑一次隔夜批次」的量級，方法跟現有pipeline完全相同，純粹是要素材數量變多
      3. **多人物身份**（誰的車／那個人是誰）：**專家原建議「同一張臉在不同圖裡保持一致做不到，要改題目成可視覺化屬性」，但使用者提出更好的解法（2026-08-09採用）**——直接沿用第1點「Pillow疊字型」的技術，在生成的人物/車圖片角落貼一個名牌文字（「山田」「田中」），完全不需要處理「同一張臉」這個做不到的技術問題：每次都可以是全新隨機生成的人物，靠名牌標識身份，不靠臉。名牌給的是「內容詞」（人名），使用者仍要自己組出完整日文句子（「あれは山田さんの車です」），跟②代換模式給提示詞（例如「会社員」）要求自己造句是同一個邏輯，不算洩題。**這樣不用改動既有題目文字**（「田中さんの車」可以照原樣保留，不用像原方案那樣繞路改寫成「紅雨傘的人」），也是「大家的日本語」教科書插畫本來就在用的手法（人物旁邊寫名字，不靠讀者認臉）。四類問題裡這個現在反而是最簡單的，優先度可以提前
      4. **空間/位置類**（廁所在哪）：不建議要求diffusion一次生成整個房間場景構圖（案例2的結構崩壞風險在多元素空間關係下只會更嚴重）。改成diffusion只離線生成一次性的簡單圖示庫（馬桶/門/箭頭等單一物件），房間佈局的空間關係完全用Pillow程式化畫（`ImageDraw` 畫房間矩形、貼圖示、疊文字標籤），座標由人工設計一次、之後可重複使用同一組圖示庫
    - ✅ **2026-08-09 問題1（書封面）跟問題3（人物身份）已實作並驗證，用真實內容替換掉原本的示範**：
      - **工作站已有Pillow（12.3.0）跟Noto Sans CJK JP字型**（`/usr/share/fonts/opentype/noto/NotoSansCJK-Bold.ttc`，index=0是JP，這個TTC同時含完整拉丁字母所以英文字標籤也能共用同一個字型檔），不用額外安裝，方案裡假設要裝的前置作業其實已經有了
      - **書封面**：diffusion生一本「素色空白書」（5候選最高10分），Pillow疊「日本語」／「ENGLISH」文字（白底黑框、置中偏上），兩張都完全清楚可讀，**徹底解決了整個討論最初卡住的「この本は日本語の本ですか」範例**，已寫入L2 qa新格式（`images/qa/l2_book_ja.png`／`l2_book_en.png`）
      - **人物身份**：實測時發現使用者的名牌方案比原方案更好，直接採用——「あの人はだれですか」生兩個人物（山田／田中），各自左下角貼名牌文字，寫入L1 qa新格式（`images/qa/l1_person_yamada.png`／`l1_person_tanaka.png`）。**過程中發現一個關於llava裁判可信度的重要案例**：兩批人物候選裡有一批（山田）5個候選全部被llava打0-1分（判斷描述用的是「a person standing」，可能太籠統讓模型沒把握），但人工下載實際看圖後品質完全沒問題（人物清楚、名牌清楚）——**llava裁判在subject描述太模糊時可能過度嚴苛，這是為什麼「最後人工抽樣」這道防線不能省略**的真實案例，不是紙上談兵的預防措施
      - 第9-13課的內容擴充當時已經審查過假名/文法，這次配圖走的是同一套「diffusion生成→llava打分→人工抽樣確認」流程，兩者互相獨立但精神一致
      - 全部11個測試檔跑過皆通過
    - ✅ **2026-08-09 問題2（開放式多變體）也已實作**：「これは何ですか」原本永遠只出現筆記本、答案可背，補了ペン/鍵/かばん三個變體（共4個），寫入L2 qa新格式。過程中5個需求裡「財布」（錢包）5個候選全部畫面糊成一團（這次是真的圖不行，llava給2分是準確的，不是像人物那次的誤判），**直接捨棄不用，維持4個變體**——不是每個物件詞都保證能穩定生成，抓不好就換一個詞，不用勉強修到能用。真實LESSONS資料驗證過50次隨機呼叫4個變體都有出現。全部11個測試檔通過
    - **問題4（空間地圖類）方向已修正，尚未實作**：使用者質疑「問路配一張房間平面圖」不自然（真實對話不會這樣），重新檢視後發現L3大部分「どこ/どちら」的答案其實是**地點名稱**（会議室です／台湾です）不是空間座標，可以直接沿用問題1/3已驗證的「單一物件+Pillow貼標籤」（例如會議室門/招牌貼「会議室」標籤），不需要原方案的圖示庫+房間排版；只有答案是「あそこ／あちら」這種相對方向詞的題目才需要指方向的簡單場景（不是完整房間平面圖）。**這個方向修正還沒實作，使用者要求先自己想想，不急著動工**
      - **下次要做問題4時的前置**：Pillow跟Noto Sans JP字型工作站上都已經有了（不用再裝，見上面問題1/3的驗證），先跟使用者確認地點名稱標籤法的方向沒問題再動工