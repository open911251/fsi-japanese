# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 專案概述

FSI（美國外交學院）口語操練法的日語自學工具。**整個應用就是單一檔案 `fsi-japanese-trainer.html`**（HTML + CSS + JS 全部內嵌），`index.html` 只是跳轉頁。無建置流程、無套件管理、無測試框架。

部署方式：GitHub Pages（repo：`open911251/fsi-japanese`，push 到 `main` 即自動部署）。

## 常用指令

無 build/lint。開發時直接用瀏覽器開啟 `fsi-japanese-trainer.html` 驗證。測試在 `tests/`（node 直接跑，會從 HTML 抽出實際程式碼測）：

```
node tests/validate-lessons.js fsi-japanese-trainer.html   # LESSONS 欄位數驗證（改教材後必跑）
node tests/test-shuffle.js fsi-japanese-trainer.html       # 隨機順序邏輯（含全 script 語法檢查）
node tests/test-feedback.js fsi-japanese-trainer.html      # 正誤回饋的正規化與相似度比對
node tests/test-srs.js fsi-japanese-trainer.html           # 間隔複習升降盒/到期/鍵還原
node tests/test-voicevox.js fsi-japanese-trainer.html      # VOICEVOX 逐拍高低/重音標籤/評分邏輯
node tests/test-homework-batch.js fsi-japanese-trainer.html # ⑩作業批改：狀態機/STT失敗降級/LLM回應解析/SRS隔離
node tests/test-voice-rotate.js fsi-japanese-trainer.html   # 多音色輪流：VOICE_POOL挑選邏輯/獨立入口重設curItemVoice
node tests/test-roleplay-ext.js fsi-japanese-trainer.html   # 角色扮演延伸對話：防跑題檢查/輪數上限
```

改動 `<script>` 內程式後至少跑一個 test-*.js（它們都先做全 script 語法編譯檢查）。測試靠字串錨點（如 `function itemsRaw`、`/* ================= 狀態`）從 HTML 切程式碼，改函式名或區段註解時要同步更新測試。

## 架構

`fsi-japanese-trainer.html` 內的 `<script>` 分幾層：

- **`LESSONS` 陣列**（約 438–1400 行）：50課教材資料，每課一個物件 `{t, g, listen, sub, qa, build}`，選填 `trans`。這是檔案的主體。**2026-08-10 課次重整**：原本28課不是跟大家的日本語逐課對應（後段課次把多課文法壓縮合併），查證大家的日本語完整50課文型清單後重整成逐課對齊，28個舊課次已有內容搬到正確新位置，另外22個新課次目前是`{pending:true}`骨架（`t`/`g`齊全但`listen`/`sub`/`qa`皆為空陣列、`build`為`null`）等內容補齊，詳細對照表在 `C:\Users\open9\.claude\plans\robust-inventing-crane.md`。**pending課次機制**：`tests/validate-lessons.js`對pending課次只驗t/g；`fillLessons()`選單disable並標「（尚未建置）」；`examMaybeUnlock()`把pending課次視為自動達標（沒有題目可測，不會卡住後續解鎖窗口推進），一次解鎖會連續跳過整串pending課次直到下一個真的有內容的課；`planNextLesson()`（每日排課推進）同樣跳過pending課次找下一個可練的課。課次重整導致舊的以課次索引的localStorage資料全部錯位，`store`定義後有一段版本檢查（比對`fsi_lessons_n`跟`LESSONS.length`），偵測到課數變了就清空`fsi_score`/`fsi_srs`/`fsi_examwin`/`fsi_exammastery`/`fsi_examweight`/`fsi_examlog`/`fsi_plan`這幾個課次索引鍵，不留殘影。
- **狀態機**：全域 `state` 物件（`mode`/`lesson`/`idx`/`running`/`runId`）。`runId` 遞增用於取消進行中的非同步播放迴圈（`sleep()` 會輪詢 `runId` 提早結束）。
- **語音層**：`say()` 優先走 `synthCloud()`（Google Cloud TTS REST API，key 存 localStorage），失敗或無 key 時退回 `speakBrowser()`（Web Speech API）。**多音色輪流（`voiceRotate`，2026-08-12，Roadmap #34）**：選填開關，開啟後`playItem()`每一題依索引輪流指定`VOICE_POOL`（6種Cloud TTS聲音）其中一種存進模組層級變數`curItemVoice`，`synthCloud()`優先讀`curItemVoice`、沒設才退回`$("voiceSel").value`——同一題內的cue/答案/示範共用一個聲音，題與題之間輪流換，訓練耳朵適應不只一種音色。因為`curItemVoice`是共享狀態，獨立於`playItem()`直接呼叫`say()`/`synthCloud()`的入口（`playModel`/`playModelSlow`/`analyzePitch`/`mpAsk`/`examAsk`/`hwPitchFor`）都要在開頭重設`curItemVoice=null`，否則會殘留上一輪代換/應答練習的輪流聲音——`tests/test-voice-rotate.js`用原始碼掃描確認這幾處都有重設。`liveModel()`的示範contour快取key也要用`curItemVoice||voiceSel`而不是隨手讀`voiceSel`，否則輪流時快取會被誤用成別的聲音的資料。
- **五種練習模式**：`playItem()` 依 `state.mode`（listen/sub/qa/build/trans）決定播放與停頓流程；`runFrom()` 是主迴圈。sub 模式的項目有三種 type：`base`（基本句）、`prev`（代換詞預習，開關控制，不進 SRS）、`cue`（正式出題）。trans（轉換操練，🔁分頁）沿用 qa 的資料格式與播放邏輯（`playItem` 的 `qa`/`trans` 是同一分支）——給一句已學過的句子＋轉換指示（肯定→否定／過去／疑問），要自己套文法規則變出來，跟代換操練的「套現成詞」不同，練的是規則內化；資料存在每課的 `trans` 欄位（選填，目前只有第1課有內容），格式跟 `qa` 完全相同（5欄 `[指示句,指示假名,答案,答案假名,中文]`）。**鏈式代換（`chain:true`，2026-08-11 試點）**：既有代換句型只留一格空、所有cue都對著同一格換，違背FSI真正的「複式代入練習」——原句其他語意自由的詞（主體、機構名等）永遠固定不變。`sub` pattern物件新增選填`chain:true`旗標的鏈式句型，`cues`彼此接續（下一句延續上一句除了被換那格以外的內容，不是每個cue都各自對著固定base句變化），需要學習者自己判斷代換詞該換哪一格、基於「上一句」（不是base句）講出新句子。因為`cues`裡每一項本來就是預先寫好的完整答案字串（不是執行時運算），鏈式內容不需要改資料格式，只是cue的寫法不同；但三處既有機制會破壞鏈式句型「必須固定順序播放、不能被抽樣/打亂/單獨抽出」的前提，已加`chain`旗標的例外：`subCapMap()`（cue數超過`subCapInput`上限時原本會`shuffle().slice()`隨機抽樣，chain句型永遠給完整`cues`）、`items()`的隨機順序分支（chain句型不受「隨機順序」開關影響，cue順序永遠固定）、`srsIdOf()`（chain句型的cue不進SRS個別複習排程，避免脫離鏈狀態被單獨抽出——這是v1的簡化取捨，不是遺漏）；`srsResolve()`同步加了對稱的防呆，讓內容改版前就已存在的殘留SRS鍵（例如某句型原本是單槽、使用者已經練出到期複習資料，之後才被改成chain）也不會被複習隊列撈出來，這是瀏覽器實測時才發現的真實缺口，不只是理論上的完整性。首個試點是第4課「デパートは十時から＿＿までです」改寫成`＿＿は＿＿から＿＿までです`（主體/開始/結束三槽輪換，cue文字前綴括號標籤如「（結束）八時」消解「時間詞該換開店還是關店」的歸屬歧義，這是內容審查抓到的真實設計缺口，不是單純風格選擇）。82組句型中audit出31組有額外可代換槽位，其餘30組是否要跟進鏈式化尚未決定，待這個試點驗證後再評估範圍。
- **錄音與腔調分析**：MediaRecorder 錄音 + Web Audio 解碼，`pitchTrack()`（自相關法）抽音高、`contour()` 正規化、`analyzePitch()` 算相似度/語速/句尾升降並畫圖。三處 `getUserMedia`（正誤回饋／⑤錄音對比／跟讀）共用 `MIC_CONSTRAINTS`（關掉瀏覽器預設的雜訊抑制／自動增益，那兩個是録音「悶」「忽大忽小」的常見成因）與 `newRecorder()`（明示 128kbps opus，避免瀏覽器預設低位元率造成破音）。
- **正誤回饋（選用）**：`fbGap()` 取代代換/應答留白的純 sleep，開啟時錄音並 POST 到使用者自填的 STT endpoint（OpenAI 相容），`fbSim()`（Levenshtein + `fbNorm()` 正字法歸一）比對正解後顯示於 `fbArea`；辨識在背景進行不阻塞播放。LLM endpoint 欄位留給開放應答模式。
- **回合評分**：`roundVerdict()`／`scoreSave()`／`scoreBadge()`／`finishRound()`（localStorage 鍵 `fsi_score`）。回饋開啟的 sub/qa 回合結束結算，`fbCheck` 會把遲到的辨識結果補進去（`roundDone` 旗標）。課程選單徽章由 `fillLessons()` 讀 `scoreBadge()`。
- **操練中腔調圖**：`fbPitch()` 在留白錄音後沿用⑤的音高管線畫到 `fbCanvas`（`drawPitch` 第三參數選擇畫布）；listen 跟讀也錄（`fbRecord()` 是共用的留白錄音器）。`worstWindow()` 找差異最大區段標紅＋`fbReplaySeg()` 半速切片重播；`fbHints()` 產生文字修正建議。即時跟唱：`liveStart()`（AnalyserNode 每 45ms 跑 `pitchOfFrame()` 自相關）在留白期間把使用者音高即時疊畫在示範軌道上（`liveDraw()`），示範 contour 由 `liveModel()` 快取；`fbRecord()` 第三參數傳句子文字即啟用。
- **聽辨訓練**：`MINPAIRS` 資料＋`mpAsk()/mpAnswer()`（⑥分頁，成績鍵 `fsi_minpair`）。四類對立：重音（acc 含 `↘`/`‾`）、長短音、促音、清濁（acc 含類別詞如「長音」「促音」「濁音」）。新增對立組時句 i 必須包含選項 i 的字、兩句載體須相同（只差目標詞），tests/test-minpair.js 會驗標記。抽題走 `mpPick()`：依 `mpHist.g`（每組答錯率）加權，錯得多的組優先出現。同分頁另有「重音位置」子分頁（`ap*` 函式群，成績鍵 `fsi_accentpos`，`ACCENT_WORDS` 詞庫），需 VOICEVOX，見下。
- **綜合測驗（⑨分頁，Roadmap #20）**：跨課隨機抽考已練過的 `sub`／`trans`／`qa`（僅配圖變體格式）題目，範圍是獨立於「目前學到第幾課」的解鎖窗口（`examWin`，localStorage `fsi_examwin`）——初始解鎖前 `EXAM_INIT`（2）課，窗口內**每一課各自**（非平均，避免表現好的課蓋掉表現差的課）正確率達 `EXAM_THRESH`（75%）且樣本數 ≥`EXAM_MINN`（4，防單次僥倖）才解鎖下一課（`examMaybeUnlock()`）。格子顏色（`examMastery`，localStorage `fsi_exammastery`）比照 `scores[].best` 只升不降——答對才升階、答錯不退階；已解鎖範圍答錯不影響顏色，但比照 `mpPick()` 的加權邏輯（`examWeight`，localStorage `fsi_examweight`）暗中提高該句型之後被抽到的機率，兩條線互不干擾。格子牆（`examRenderAll()`）畫出全部**已建置**課次（跳過`pending:true`骨架課次，不顯示成空格子）的全貌，未解鎖顯示灰格。`examAsk()` 沿用 `sttTranscribe`/`fbSim` 判定對錯（需先填 STT URL），不經過 `runFrom`/`items()` 主狀態機（跟⑥聽辨一樣是獨立自成一體的分頁）。`examPoolKeys()` 的 qa 只收 `{q,qk,variants}` 配圖變體格式（`qaResolveItem()` 能給出確定答案），舊5欄陣列格式的 qa（答案是編出來的劇本事實，沒圖無從推理）仍然排除在題庫外；配圖變體格式還要通過 `examQaGradable()`（任兩個變體答案的 `fbSim` 相似度都低於 `EXAM_THRESH`本身才放行，刻意不留保守margin——門檻以下的組合在真實評分流程裡本來就不會被誤判過關，多留margin只會無謂縮小題庫）——2026-08-09 實測發現第3課電梯題「あちらです」跟「こちらです」相似度高達0.8、超過判定門檻0.75，代表使用者講反方向仍可能被判對，這種答案組不適合自動評分，日常③應答練習不受影響（那邊只播正解不判對錯）；同一次檢查意外抓到第二個真bug——L1「あの人はだれですか」的山田さん／田中さん人物身份變體相似度同樣是0.8，一併排除。`examPick(exclude)` 支援傳入一個已抽過的key的Set，單輪測驗內優先抽沒問過的題、池子抽完才解禁重複（`examAsk()` 用全域 `examSeen` 追蹤，跟 `examSessionN` 一起在達到 `EXAM_SESSION_LEN` 時重置）——這是因應「小題庫＋答錯4倍權重會讓同一題在同一輪內被反覆抽中」的體感問題，跟既有的答錯加權機制並存，不是取代它。——這是 2026-08-09 的分階段修正：先移除整個 qa 題型解決「測驗答不出來」的問題，等配圖變體機制做出來後才把「已解決可測驗性」的子集放回題庫，`examResolve()` 對 qa 呼叫 `qaResolveItem()` 拿到含 `img` 的結果，`examAsk()` 用 `showExamImage()`（獨立於③應答頁的 `showQaImage()`，各自對應 `#examImg`/`#qaImg`）在出題框顯示圖片。內部除錯 log（`examLog`，localStorage `fsi_examlog`，FIFO上限500筆）記錄逐次事件含判定原始訊號（相似度、當時門檻、STT辨識文字），**不對使用者顯示**，供事後核對評分邏輯用。⑥聽辨頁同批加了 `mpWallRender()` 卡片牆（依 `mpHist.g` 答對率上色，跟綜合測驗共用同一套 `.mastery0-3` CSS 色階）。**已知限制（2026-08-10課次重整後更新）**：課次已擴展到50課對齊大家的日本語，其中20課是`pending:true`骨架（無內容，格子牆不顯示），已建置的30課裡後段課次多半仍是每課僅1句型4代換詞的最低骨架量，這些課的格子牆會偏稀疏；此為已知前置依賴，非本次遺漏，進度與對照表見 `C:\Users\open9\.claude\plans\robust-inventing-crane.md`。
- **VOICEVOX 音高分析（選用）**：設定卡 `vvUrl`/`vvSpeaker`（localStorage `fsi_vv_url`/`fsi_vv_speaker`）。`vvQuery()` 呼叫 `/audio_query` 取得整句逐拍（mora）結構，`vvMoras()` 展平成 `{kana,voiced,hilo,t0,t1}` 陣列——高低不查教科書規則表，改用「該 accent phrase 內有聲拍音高是否高於該 phrase 平均」（引擎實際輸出常有「尖峰延遲」，比規則表更貼近使用者聽到的示範，見 tests/test-voicevox.js 的真實 fixture）。`moraScore()` 拿使用者 `contour()` 的 48 點序列依 t0/t1 逐拍取平均比對，標記吻合／不吻合，`renderMoraRow()` 畫成一排色塊；`drawMoraTrack()` 在 `drawPitch()`/`liveDraw()` 的畫布疊金色理論高低階梯。三處掛點：`fbPitch()`（操練中回饋）、`analyzePitch()`（⑤錄音對比）、`liveModel()`/`liveStart()`（卡拉OK跟唱）。`vvSynthesize()` 可把改寫過 `accent_phrases[i].accent` 的 JSON 重新合成，供⑥「重音位置」子分頁出題（任一詞查真實重音、複製 JSON 改成另一個重音位置當錯誤選項，不需人工找同音詞對）。VOICEVOX engine 需以 `--cors_policy_mode all` 啟動才允許瀏覽器跨源呼叫；示範播放仍走 Google TTS，VOICEVOX 只當分析器。**拍長分析（`moraDurationAligned`，2026-08-12，Roadmap #34）**：既有的`moraScoreAligned()`只比音高，抓不出「長短音混淆但音高曲線恰好像」這類錯誤（訊號在時長不在音高）。沿用同一套`dtwPath()`DTW對齊，但數的不是音高值而是「DTW路徑分配到每個ref index範圍的使用者contour點數」，這個計數是「使用者花了多少相對時間在這一拍」的代理訊號，跟參考音的預期時長佔比一比，比例明顯偏離1（門檻1.4/0.7）標記「長」或「短」。**只有DTW對齊成功時才有這個訊號**——`moraScore()`比例對齊的退路版本本身就是假設使用者跟參考音等比例對齊，沒有獨立時長訊號可比較，`vvScore()`回傳的每拍物件多了`dur`欄位，比例對齊時固定是`null`，呼叫端（`renderMoraRow()`）要能接受這個欄位缺席。門檻（1.4/0.7）不是套教科書長短音2:1比例訂的——用`tests/test-voicevox.js`既有的「拖長第1拍到54%」fixture（該測試檔本來就拿這組當「明顯有問題」的參照案例）反推校準，因為教科書比例（1.8/0.55）連這個已知案例都抓不到（實際算出來只有1.65倍）。**只驗證過合成音，沒有真人錄音的實測校準**，門檻之後可能需要使用者自己有VOICEVOX環境時拿真實錄音調整，程式碼註解跟ROADMAP都如實記錄這點。
- **今日條**：`todayRender()`／`markPractice()`（streak）／`srsDueAll()`／`lastPracticed()`／`tdPanel()`（漸進揭露）。localStorage 鍵 `fsi_daily`（跨日歸零）與 `fsi_streak`。`finishRound()` 呼叫 `markPractice()`。
- **今日建議（`tdAdvice*`，2026-08-11 新增）**：今日條上的「📋 老師：今天學什麼？」按鈕，讓老師（LLM，跟⑩作業批改同一個固定「認真母語教師」身分，跟`teacherChat*`可自訂的人設脫鉤）依活動紀錄給簡單的練習規劃（哪幾課、哪個功能、練多久），純文字講評不打分數。`tdAdviceGatherContext()`只讀`streakD`/`plan`/`srsDueAll()`/`lastPracticed()`/`scores`/`daily`這些既有訊號組成一段文字丟給LLM，**刻意完全不碰`examLog`/`examWeight`/`examMastery`/`examWin`這些⑨綜合測驗的資料**——使用者明確要求兩者分開，綜合測驗是給想自我挑戰、往下探索的人用的獨立功能，不該被每日練習規劃牽動、也不該反過來被這個功能影響。`tests/test-today.js`用精確的屬性存取正規式（`examLog[.\[]`等，只比對真的讀取，不誤觸發只是在註解裡提到名字說明「不碰」的情況）掃描`tdAdvice`專屬區塊確認這個決定沒被違反，是這次順便修好的測試設計坑。
- **間隔複習**：`srs*()` 函式群（Leitner 五盒，localStorage 鍵 `fsi_srs`，每句鍵＝`mode|lesson|id`）。`runFrom(0)` 時 `srsBuildReview()` 把到期句插到 `state.review`（items() 會 concat 在最前）；升降盒由 `fbCheck`（回饋開啟）或 `runFrom` 迴圈（關閉）呼叫 `srsMark()`。句子識別用物件參照 `indexOf`（`srsIdOf`），所以隨機順序下也正確。
- **自訂教材**：使用者貼句子後產生 `state.customLesson`（`lesson === -1` 時使用）。
- **影子跟讀（⑦分頁）**：使用者選本機資料夾（`webkitdirectory`，不上傳、不持久化），`shadowClassify(name,relPath)` 依檔名決定分類——大家的日本語風「第N課」命名走課號＋小節關鍵字（`shadowSectionOf()`），課號對齊本工具課程順序；其他沒有這種命名的教材（跟讀書、文法書等）退回用資料夾結構（上層資料夾名）分類。這顆函式同時被「選資料夾」（`shadowIndexFiles()`）跟「匯入分句結果」共用，保證兩條路徑算出同一組 key。A/B 區間可手動設，也可以按「自動偵測分句」把整段音檔丟給 STT（`sttTranscribeFull()`，server 需回傳 `segments`，見下）拿到逐句時間戳＋文字，結果存 localStorage（`shadowSliceKey()`，格式跟 `shadowTextKey()` 一樣是課號＋小節當 key）；`shadowSelectSeg()`／`shadowStepSeg()` 做逐句導覽（上一句／下一句按鈕）。批次預先分析好的結果可以用「📥 匯入預先分析的分句結果」讀外部 JSON manifest（陣列 `[{path, segments}]`，`path` 是選資料夾時會出現的 `webkitRelativePath` 格式）寫進同一套 localStorage 快取，不用在瀏覽器裡一個個音檔重跑。「🎙 跟讀（播放＋錄音）」同步播放選定區間並錄音，播放結束後留 1.5 秒緩衝才真的停止錄音（跟讀常慢半拍，避免尾音被切）。「🔍 講評」重用⑤錄音對比的整套音高管線（`pitchTrack`/`contour`/`drawPitch`/`worstWindow`/`vvMorasFor`/`vvScore`），但參考音直接用 `sliceBuffer()` 從這段真人音檔本身裁出來，不叫 Google TTS、不需要 API key；`worstWindow` 抓到的最大差異段要換算回整軌的絕對秒數（`shadowSeg.t = shadowA + A.t0 + ...`，因為參考音的 `contour().t0` 是相對裁切後片段起點算的）才能讓 `shadowReplaySeg()` 半速重播對到正確位置。
- **角色扮演（⑧分頁，最小版）**：`ROLEPLAY_SCENARIOS` 資料刻意放在「/\* ================= 狀態」註解**之後**（例如跟 `MINPAIRS` 放一起）——`tests/test-shuffle.js` 用 `lastIndexOf("];", indexOf("狀態"))` 抓 `LESSONS` 陣列收尾，如果在 `LESSONS` 收尾跟狀態註解中間插新陣列會被誤判進 `LESSONS` 的抽取範圍，改資料時要留意這條邊界。每情境依課程文法程度分四級、固定兩輪；`turns[i]` 是 `{ask, cue, accept}`：`ask` 是給 LLM 的指示（這輪要問什麼，不是逐字稿）、`cue` 是顯示給使用者的中文提示詞（不是日文答案）、`accept` 是可接受的日文寫法陣列。`rpAskTurn()` 呼叫 LLM 即時生成 AI 問句，生成後用 `rpValidQuestion()` 檢查有沒有直接洩漏 `accept` 內容，洩題就帶提醒重問（最多 3 次嘗試）才顯示給使用者。使用者作答用 `fbNorm()` 正規化後對 `accept` 做子字串比對（`rpSend()`）算對錯，不再打一次 LLM 當裁判——自架小模型當裁判本身不穩，關鍵字比對更確定也不用等。跟「進度陪伴角色」（`teacherChat*`）完全獨立，不共用歷史／persona，狀態只留在記憶體不寫 localStorage（換分頁再切回來歷史還在，重新整理頁面才會消失，按「開始」會重置）。**延伸對話（`rpExt*`，2026-08-12，Roadmap #35）**：2輪劇本結束後選填的延伸練習，解決「角色扮演雖然AI即時生成問句但內容完全照劇本走、不算真的跨進FSI操練階序的Communicative層級」這個缺口。`rpExtAskTurn()`不再用`turn.ask`腳本，改用要求LLM「引用使用者剛講的具體內容」的指示生成追問；不判對錯（`rpLogEntries`裡的使用者發言沒有`hit`欄位，`rpRender()`據此不畫✅❌），`RP_EXT_CAP`（4輪）當上限、`rpExtStopBtn`讓使用者隨時喊停，兩者並存。`rpExtValidQuestion()`防的不是洩題（延伸階段沒有預設答案可洩，這個概念不成立），是防跑出情境/程度範圍（非日文字元比例、句長上限）。延伸對話結束後才一次性質性講評（比照`hwSystemPrompt()`固定「認真母語教師」身分，跟角色扮演本身的情境人設脫鉤），不即時判——複用⑩作業批改已驗證過的批次回饋pattern。
- **作業批改（⑩分頁，2026-08-10 新增，`hw*` 函式群）**：使用者做5題`qa`模式練習錄音，全部做完（或至少1題）才一次「繳交」，不像③応答練習每題即時判對錯——這是「作業不是考試」定調的功能，不打分數、不用逐字比對正解，只給質性的文法/腔調建議。`hwState` 是獨立小狀態機（比照角色扮演／跟讀模式的慣例：純記憶體不寫 localStorage，重整頁面就消失，切分頁再切回來狀態還在），錄音沿用 `newRecorder()`/`MIC_CONSTRAINTS`，但跟 `fbRecord()`「錄完立刻處理」不同，`hwToggleRec()` 錄完只是把 blob 存進 `hwState.answers[i]`，等繳交時才批次處理。繳交（`hwSubmit()`）流程：逐題 `sttTranscribe()`（失敗標記 `sttOk:false`、文字改placeholder「(未辨識到語音)」但仍繼續往下走，不中斷整批）→ `hwPitchFor()`（沿用 `fbPitch()` 同一套腔調管線 `contour`/`pitchTrack`/`vvMorasFor`/`vvScore`/`worstWindow`/`fbHints`/`drawPitch`，只是畫到動態 canvas `#hwCv{i}` 而非固定 `#fbCanvas`；沒填 Google TTS key 時跟 `fbPitch()` 一樣靜默回 null，不影響其餘部分）→ 全部STT/腔調做完後才一次呼叫LLM（`hwGradeCall()`，5題塞進同一個user prompt，不是5次個別呼叫），因為這個工具目前唯一的LLM用法（`teacherChatCall()`）就是純文字沒有JSON-mode依賴，這裡沿用同一個假設：LLM回應用固定中文標題分段（`第1題`/`第2題`/…/`總評`），`hwParseGradingResponse()`（純函式）用正則切分，格式跑掉就整包退回「總評」顯示、不拋例外。**`hwSystemPrompt()` 刻意跟 `teacherSystemPrompt()`/`fsi_persona_text` 脫鉤**——沒有名字沒有人設風格，固定「認真母語教師」身分，避免使用者自訂的（可能很鬆散/搞笑的）人設語氣稀釋掉批改準確度。**`markPractice()` 在使用者開始錄第一題（`hwStart()`）就呼叫，不等批改完成**（批改可能失敗/慢，streak不該被LLM在不在線卡住）；**SRS完全不碰**（不呼叫 `srsMark()`/`srsBuildReview()`），理由跟情境不明確的答案被排除在 `examQaGradable()` 外一樣：質性回饋沒有乾淨的1/0/-1訊號，硬湊會污染SRS既有假設——這個決定寫進了 `tests/test-homework-batch.js` 當回歸測試（直接掃描原始碼不含 `srsMark(`/`store.set(` 呼叫，另外跑一輪真實流程斷言 spy 從未被呼叫）。失敗降級：LLM連不上時腔調/STT結果照樣先顯示，`hw.gradeError` 顯示可重新送出（`hwRetryGrade()`）的提示，不整批卡死；未填 `llmUrl` 時明確提示「尚未設定」而非靜默失敗。`switchMode()` 離開⑩分頁時呼叫 `hwStop()`（bump `hwState.runId`，比照 `stopRun()` 用 `runId` 取消進行中錄音的既有模式），避免使用者切走後錄音殘影寫入已經離開的那批。

`server/` 是自架 STT/LLM 後端（跑在使用者的遠端工作站，非本 repo 部署範圍）：`server.py` 為 FastAPI（faster-whisper ＋ Ollama CORS 代理，只綁 127.0.0.1:8788），`start.sh` 為啟動腳本。`/v1/audio/transcriptions` 除了 `text` 也回傳 `segments`（`[{start,end,text}]`，faster-whisper 內建 VAD 切出來的整句時間戳），供跟讀分頁的自動分句用；只讀 `text` 的舊呼叫方不受影響。改動後需手動部署到工作站。

`bot/` 是日語陪聊 Discord bot（獨立於網頁工具，同樣跑在筆電＋工作站，設計見 `bot/DESIGN.md`）。**token、對話記憶、視窗紀錄等執行期資料一律放 `bot/data/`（已 .gitignore），絕不 commit**——本 repo 是 public。

## 教材資料格式（修改 LESSONS 時必須遵守）

- `listen`：每項 3 欄 `[日文, かな, 中文]`
- `sub`：每個練習為 `{p, base, cues}`；`base` 3 欄，`cues` 每項 4 欄 `[提示詞, 日文, かな, 中文]`。選填 `chain:true`（鏈式代換，見上）：`cues`必須依序接續（每項答案是「上一項答案只改一格」的結果，不是都對著`base`各自變化），寫的時候要自己逐項推演確保鏈條前後兜得起來；提示詞建議加註要換哪一格（如「（結束）八時」），避免代換詞本身無法判斷該換句中哪一格
- `qa`：每項 5 欄 `[問題, 問題かな, 答案, 答案かな, 中文]`；**選填的配圖變體格式**（Roadmap #21）：`{q, qk, variants:[{img, a, ak, cn, accept?}, ...]}`——`q`/`qk` 是題目（不隨變體變），`variants` 至少要放兩個對立版本（例如日文書封面／英文書封面各一版），播放時 `qaResolveItem()` 隨機選一個變體決定要顯示的圖跟正確答案，圖片路徑用 repo 內相對路徑（`images/qa/...`，圖片素材另外存檔案不內嵌 base64，見下）。**這個變體格式只能用在答案沒有邏輯/文法規則可推導、需要配圖才有確定答案的題目**（例如「這是日文書嗎」），不要用在敬語應對/確認請求這類答案本來就由文法規則決定的題目（純文字5欄格式繼續用，不用配圖）。yes/no類問題**一定要生對立變體**（is/isn't 兩種都要），不能只做單一固定劇本版本，否則使用者可以不看圖背答案矇混過關——這是實測後才發現的坑，仔細記錄在 [[fsi-tool-qa-image-pipeline]]。**2026-08-12新增設計原則（Roadmap #31）**：如果`ですか`是非形式本身不是這一題要測的文法點（真正要測的是配圖決定的另一件事，例如圖是哪種書、哪種狀態），問題要設計成開放式wh疑問句或「Aですか、Bですか」並列問句（比照大家的日本語課本「練習B」的提示模板精神），讓兩個變體的`a`都是肯定形完整句——這樣才能徹底排除「否定原句」跟「正面陳述另一事實」兩種都合法但`fbSim()`算出來很不像的分歧點，不用多存替代答案也不用改程式碼。「Aですか、Bですか」並列時要注意：兩個述語詞性相同（都是動詞/都是名詞）才自然道地可以直接並列（如「開いていますか、閉まっていますか」）；詞性不同（動詞vs名詞）要加それとも銜接才不生硬（如「結婚していますか、それとも独身ですか」）。但如果是非形式本身就是這一課的文法焦點（如L1/L20的です/じゃありません、だ/じゃない），或ええ/いいえ只是對話裡的自然附和語氣詞不是結構性分歧點（如L43樣態そう那兩題），維持原本的yes/no形式，不用套這條原則。**qa題庫加厚（2026-08-12，Roadmap #32）**：⑩作業批改實測發現每課qa題庫只有4-5題，`hwPickItems(5)`每次幾乎把題庫整包抽完、毫無隨機空間，5題作業每次看到的都一樣。這是題庫太薄的問題不是抽樣bug，治本方案是加厚（比照`sub`代換詞加厚模式），目前L1-8試點完成（4-5→8題），其餘課次尚未跟進。**qa答案支援多種合法講法（`accept`，2026-08-12，Roadmap #33）**：FSI專家諮詢後確立的正式做法——qa新增第三種形狀`{q,qk,a,ak,cn,accept:[[替代答案,替代假名],...]}`（無配圖但容許多種合法講法，跟純5欄陣列格式、配圖變體格式並存；配圖變體的每個variant也可選填`accept`）。`qaResolveItem()`統一正規化出`accept`欄位（未填回空陣列）。新增共用函式`fbBestSim(heard,expected,kana,accept)`：對正解/假名/每組替代講法都取`fbSim`、回傳最大值，`fbCheck()`與`examRecordAndCheck()`（⑨測驗共用比對邏輯）都改呼叫這個函式，`accept`從`playItem()`/`examResolve()`/`examAsk()`一路從資料層傳到判分層。`examQaGradable()`（配圖變體答案彼此太像時排除出題庫）同步擴大比對範圍到「每個變體的正解+所有替代講法」，只比不同變體之間（同一變體自己的正解跟替代講法本來就該像，不比）。**這不是要把每題都改到只剩一種答案去遷就判分工具（那是#31做的、FSI專家認為是本末倒置的權宜之計）**，是反過來讓判分工具能吃下「同一題本來就有不只一種合理講法」這個事實，內容編寫時心力該放在窮舉合理講法，不是窄化題目。
- **圖片素材**：另存 repo 內 `images/` 資料夾、用相對路徑引用（不用 base64 內嵌進 HTML）——這是唯一在圖片數量規模（估計上百張）下可行的做法，`<img src="images/...">` 不需要建置流程、不依賴外部CDN，跟「不拆分檔案」規則真正想擋的東西（拆分程式邏輯/依賴建置工具鏈）不是同一件事
- `build`：`{full, parts}`；`full` 3 欄，`parts` 為由短到長的字串陣列
- `trans`（選填，轉換操練用）：格式跟 `qa` 相同，每項 5 欄 `[指示句, 指示かな, 答案, 答案かな, 中文]`，指示句要是完整可唸的日文句子（例如「「わたしは学生です」を否定形にしてください。」），不要只寫「→否定形」這種符號，TTS 會照著唸
- **改完教材必須用 node 驗證欄位數和 JS 語法**，欄位數錯了播放流程會壞掉

## 擴充教材的標準流程（新增 sub 句型／trans 題目都照這個走，不要每次重新發明）

1. 比對該課 `g` 欄位列的文法點，找出 `sub`（或 `trans`）裡完全沒被操練到的項目——只是既有句型的表層變化（例如加疑問詞尾）不算缺口，語序/結構真的不同才算。
2. 依現有課文風格寫草稿：原創句子（不抄課本）、跟該課 `listen`/`sub`/`qa`/`build` 現有例句不撞句、難度不超過該課範圍。
3. 丟給**獨立 agent**（不共用草稿時的思路/上下文）扮演母語日語老師，逐句核對文法、假名讀音、語感自然度、有沒有撞句——不要自己審自己的草稿。
4. 依審查結果修正後才寫入 `LESSONS`，跑 `node tests/validate-lessons.js` 與 `node tests/test-shuffle.js` 確認格式與語法沒壞。
5. 一次只做小批（2-8課或1-2課）試點，不要一口氣做完全部課次。

**目前進度**（2026-08-10 更新）：**全50課骨架已補完**（過程見ROADMAP #22），**代換詞加厚也已全部完成**（過程見ROADMAP #23）：第4-50課共68組句型從最低4個加厚到約8個，對齊第1-3課的深度，全教材代換詞總數390→586。過程中八批全數經獨立審查，抓到多處真問題（跨課撞句、假名讀音錯誤、語言學實質錯誤如尊敬語/委婉語混淆等）並修正，全部11個測試檔通過。**過程中發現並修好一個測試盲點**：`tests/test-shuffle.js`原本假設cue數不會超過`subCapInput`預設上限4（因為之前所有課次剛好都≤4），加厚後暴露出測試沒考慮`subCapMap()`會截斷的邏輯，已改用`subCapMap()`回傳值當比對基準修正（不是內容錯誤，是測試假設過時）。`trans` 轉換操練目前只有第1、2課有內容，其餘課次還沒做，是下一個可能的擴充方向（未經使用者授權開工，不要自行啟動）。**2026-08-10 全系統多角度審查後又補了一輪（過程見ROADMAP #25）**：抽樣跨全部50課請FSI教學法專家＋日語母語教師審查，抓到「何で」讀音重複錯誤（L5/L7，なんで→なにで已修正）跟一個結構性缺口——6課（L15/25/30/35/40/50）的`g`文法點清單列了多項，但`sub`只操練其中1項，其餘文法點只在`qa`要求冷產出，違反FSI「從操練過的句型產出」原則，已補上8組新句型（72句）補齊操練覆蓋，全corpus撞句掃描確認無新增撞句。全教材句型數74→82、代換詞總數586→651。**2026-08-11 鏈式代換試點（過程見ROADMAP #27）**：使用者發現既有代換句型只換單一固定槽位（如L4「デパートは十時から＿＿までです」8個代換詞全部只換閉店時間），違背FSI真正的複式代入練習精神，audit全82組句型後找到31組有額外可代換槽位。新增`chain:true`旗標支援鏈式代換（見上方架構說明），第4課「デパート」句型作為試點改寫，內容經獨立審查兩輪（第一輪抓到時間詞歸屬歧義與學校時段語意不自然，修正後加了括號槽位標籤才過關）。**2026-08-11～12 FSI專家深度分類＋批次A（過程見ROADMAP #28）**：請FSI專家給出多槽設計的具體上限（2槽為主、3槽硬上限，總步數8-12最多15，主槽6-8/次槽2-4，開槽要對應`g`欄位文法點、不是能挖就挖），再用這套判準逐一分類全82組句型——結果只有4組真正值得開槽（L2/L3/L10/L22）＋5組低優先（L1×2/L5/L7×2），其餘約73組維持單槽（多半是L14後的純動詞活用示範，強加槽位違反FSI「一次一變項」）。**這批的內容設計走的是「獨立多槽」（各代換詞各自對base句代換，互不連鎖），不是`chain:true`鏈式**——判準是槽位之間有沒有「語意連貫故事線」，這9組都沒有（例如「換交通工具」跟「換目的地」彼此無關），所以完全不需要動`subCapMap`/`items`/`srsIdOf`那三處程式碼，純粹是內容層的事。**審查抓到一個新的坑，記錄下來避免下次重犯**：第一輪草稿把「換句子裡不同位置」的代換詞直接塞進既有句型的同一個`cues`陣列（例如L1「わたしは＿＿です」的空格是職業詞，卻塞進換主體的「田中さん」），跟句型`p`標記的空格對不上，使用者可能套錯位置卻因為文法仍合法而毫無自覺（最壞案例「田中さんは佐藤さんじゃありません」語意完全通順、無法自我糾錯）。修法：**這種情況不要塞進同一個`cues`，另開一個獨立的`{p,base,cues}`句型物件**（這個工具本來就有「一課多個句型」的慣例，比照辦理即可，不需要引入`chain`或多空格`p`的機制）；只有在新代換詞是**深化同一個既有空格**（例如L10「猫は＿＿にいます」原本cue是「物品+方位詞」整包，新cue固定物品只變方位詞，仍是同一格）時才維持同一個`cues`陣列，並視情況加括號標籤消歧義。**2026-08-12 批次B（過程見ROADMAP #29）**：接續批次A分類時撿到的附帶發現，補齊L18/36/42/44/46/47共11組`sub`覆蓋缺口（`g`欄位列多個文法點但`sub`只操練1項，跟ROADMAP #25修過的L15/25/30/35/40/50同一類問題，這6課當時沒被掃到）。過程中確認`cue`的`c[0]`（代換詞顯示文字）會被`say()`實際唸出來，不是純畫面文字——L44「やすい/にくい」這組因為兩種說法對每個動詞都自然常用、不像な形容詞接續そうです/ようです/はずです那樣由詞性規則唯一決定答案，代換詞前綴括號標籤（如「（にくい）書きます」）才有唯一正解，這連帶驗證了鏈式代換沿用的同款標籤格式在TTS唸讀上是安全的。**2026-08-12 L4從鏈式改回獨立多值（過程見ROADMAP #36）**：使用者反覆討論後確認「靈活比較重要」——核心問題是每個槽位有沒有被換到，不是換的次數夠不夠多，鏈式最大的代價（cue沒辦法個別進SRS複習）沒有對應的好處值得換。L4的「デパートは＿＿から＿＿までです」直接改成3機構×3開始×3結束＝27種獨立組合（自由交叉，不用`chain:true`，每個組合都是`cues`裡預先寫好審過的完整句子，不是runtime動態拼接），**目前全教材已經沒有任何句型在用`chain:true`**——`chain`旗標本身（`subCapMap`/`items`/`srsIdOf`/`srsResolve`四處的例外邏輯）還留在程式碼裡，是通用機制，之後如果真的有適合鏈式敘事連貫故事線的句型還是能用，只是現在沒有內容在用而已，不要因為看到程式碼有這段就以為現在有課次在跑鏈式。**2026-08-12 批次A重做為獨立多值交叉（過程見ROADMAP #37）**：L4試點成功後使用者要求把同樣的「獨立多值自由交叉」設計套到當天稍早批次A新增的9組句型上（L10當時是既有槽位加深、性質不同，排除在外），改寫L1（です/じゃありません形各一組）、L2、L3、L5、L7（あげます/もらいました各一組）、L22共8組交叉、86個組合。3組獨立審查抓到L5「電車で駅へ行きます」語意循環（駅本身就是電車的移動終點，跟L4銀行案例同一種修法——整個換掉目的地候選值而非單獨調整）、L22子句×名詞交叉連續兩輪動詞-受詞相容性問題（読む只適合書本→改使う又不適合書本→最終改成通用態度動詞気に入っていた才解決）、L2「これは本です」跟既有listen[0]真的撞句（collision checker抓到後從cross product排除這一格）；另外自己在寫collision checker時抓到一個腳本bug（`replacing`排除表漏列L1的兩個索引）。全數修正後13個測試檔通過。

## 規則

1. **維持單一 HTML 檔架構**：不拆分檔案、不引入任何外部依賴（CDN、npm、框架都不要）。
2. **教材句子必須原創**：依「大家的日本語」的文法進度編寫，但**勿抄課本原句**（有版權）。
3. **絕不把 API key 寫進程式碼或 commit**；也不要向使用者索取任何憑證。Key 只存在使用者瀏覽器的 localStorage。
4. **回覆使用繁體中文**，簡潔直接。
